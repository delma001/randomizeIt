# randomizeIt Package

This package includes the Shiny interactive app used in EPsy 5261 at the University of Minnesota.

<br /><br />

## Installation

To install this package you will first need to install the **devtools** package from CRAN. Once **devtools** is installed then you can install the **randomizeIt** package by typing the following syntax in the RStudio Console window:

```
devtools::install_github("delma001/randomizeIt")
```

<br /><br />

## Opening the Shiny App

To run the shiny app, type the following syntax into the RStudio Console:

```
# Load library
library(randomizeIt)

# Run app
runRandomizeIt()
```

